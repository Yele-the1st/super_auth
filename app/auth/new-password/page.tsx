import NewPasswordForm from "@/components/auth/new-password-form";
import { FC } from "react";

interface pageProps {}

const NewPasswordPage: FC<pageProps> = ({}) => {
  return <NewPasswordForm />;
};

export default NewPasswordPage;
